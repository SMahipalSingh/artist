# Leave empty - in single-service Render deployment, Express serves the React build
# and all /api/* calls go to the same origin (relative paths work).
# Only set this if deploying frontend separately (e.g., Vercel).
VITE_API_URL=
